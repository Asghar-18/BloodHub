<!DOCTYPE html>
<html lang="en">

<head>
     <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blood Donation Information</title>
    <link rel="icon" href="../Icons/blood-drop.png">
    <!-- <link rel="stylesheet" type="text/css" href="styles/styles.css"> -->
    <style>
        /* Your CSS styles go here */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }

        /* Header Styles */
        .header {
            background-color: #4a90e2;
            color: #fff;
            /* padding: 10px 0; */
        }

        .header .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
            display: flex;
            justify-content: space-between;
            /* Separates logo and navigation */
            align-items: center;
        }
        .header-logo {
            font-size: 24px;
            font-weight: bold;
            color: #fff;
            text-decoration: none;
        }

        .header-nav {
            display: flex;
            list-style: none;
            margin: 0;
            padding: 0;
        }


        .header-nav a {
            text-decoration: none;
            font-weight: bold;
            color: #fff;
            padding: 10px 15px;
            border-radius: 5px;
            transition: background-color 0.3s;
        }

        .header-nav a:hover {
            background-color: #357abd;
        }



        /* Inventory Section Styles */
        .inventory {
            padding: 20px;
        }

        h1 {
            font-size: 28px;
            font-weight: bold;
            margin-bottom: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }

        table,
        th,
        td {
            border: 1px solid #ccc;
        }

        th,
        td {
            padding: 10px;
            text-align: center;
        }

        table th {
            background-color: #4a90e2;
            color: #fff;
        }

        /* Add box-shadow to the table */
        table {
            box-shadow: 5px 5px 10px rgba(0, 0, 0, 0.1);
        }

        /* Use icons or images to represent the blood types in the table */
        .blood-type {
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .blood-type img {
            width: 20px;
            height: auto;
            margin-right: 5px;
        }

        /* Footer Styles */
        .footer {
            background-color: #4a90e2;
            color: #fff;
            text-align: center;
            padding: 10px 0;
            position: absolute;
            bottom: 0;
            width: 100%;
        }

        .footer .container {
            max-width: 1200px;
            margin: 0 auto;
        }
    </style>
</head>

<body>
  <header class="header">
      <div class="container">
          <a href="/asghar" class="header-logo">💉 BloodCare</a>
          <nav class="header-nav">
              <a href="/asghar">Home</a>
              <a href="/Appointment">Appointment</a>
              <a href="/about">About</a>
              <a href="/contact">Contact</a>
          </nav>
      </div>
  </header>

  <section class="inventory">
      <div class="container">
          <h1>Inventory Management</h1>
          <table>
              <caption>Blood Inventory Status</caption>
              <thead>
                  <tr>
                      <th>Blood Type</th>
                      <th>Units Available</th>
                  </tr>
              </thead>
              <tbody>
                <% data.forEach(function(item) { %>
                    <tr>
                        <td><%= item.blood_type %></td>
                        <td><%= item.units_available %></td>
                    </tr>
                <% }); %>                
              </tbody>
          </table>
          <p>Note: Alerts and notifications for low blood supply will be displayed here.</p>
      </div>
  </section>

  <footer class="footer">
      <div class="container">
          &copy; 2023 BloodCare. All rights reserved.
      </div>
  </footer>
</body>
</html>